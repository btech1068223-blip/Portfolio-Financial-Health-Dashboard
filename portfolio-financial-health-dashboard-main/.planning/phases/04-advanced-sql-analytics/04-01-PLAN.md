---
phase: 04-advanced-sql-analytics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [sql/query_yoy_growth.sql, sql/query_margins.sql, sql/query_decel.sql]
autonomous: true
requirements: [ANLT-01, ANLT-02, ANLT-03, ANLT-04]
must_haves:
  truths:
    - "query_yoy_growth.sql runs successfully against database and returns YoY growth metrics"
    - "query_margins.sql runs and returns 4-quarter rolling averages for gross and operating margins"
    - "query_decel.sql runs and returns list of companies with consecutive deceleration"
  artifacts:
    - path: "sql/query_yoy_growth.sql"
      provides: "YoY Growth SQL Query"
      contains: "LAG"
    - path: "sql/query_margins.sql"
      provides: "Rolling Margins SQL Query"
      contains: "AVG"
    - path: "sql/query_decel.sql"
      provides: "Deceleration detection SQL Query"
      contains: "LAG"
  key_links: []
---

<objective>
Write advanced SQL analytical queries for metrics calculation.

Purpose: Implement clean standalone SQL scripts for YoY growth, rolling margins, and growth deceleration.
Output: sql/query_yoy_growth.sql, sql/query_margins.sql, sql/query_decel.sql.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sqlite-storage-schema-setup/03-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Create sql/query_yoy_growth.sql</name>
  <files>sql/query_yoy_growth.sql</files>
  <action>Write YoY growth SQL query using standard window functions like LAG(..., 4) on quarterly data to compare a calendar quarter with the same quarter in the previous year.</action>
  <verify>sqlite3 data/financial_health.db < sql/query_yoy_growth.sql</verify>
  <done>Query executes and outputs YoY growth rows</done>
</task>

<task type="auto">
  <name>Create sql/query_margins.sql</name>
  <files>sql/query_margins.sql</files>
  <action>Write rolling average gross margin and operating cash flow margin query. Calculate 4-quarter rolling averages using SQLite window function AVG(value) OVER(PARTITION BY ticker ORDER BY date ROWS BETWEEN 3 PRECEDING AND CURRENT ROW).</action>
  <verify>sqlite3 data/financial_health.db < sql/query_margins.sql</verify>
  <done>Query executes and outputs rolling margin averages</done>
</task>

<task type="auto">
  <name>Create sql/query_decel.sql</name>
  <files>sql/query_decel.sql</files>
  <action>Write deceleration detection query. First calculate YoY growth rate per quarter, then check if growth is lower than the previous quarter, and check if this deceleration occurs for 2 or more consecutive quarters.</action>
  <verify>sqlite3 data/financial_health.db < sql/query_decel.sql</verify>
  <done>Query executes and returns decelerating companies list</done>
</task>
</tasks>

<verification>
- [ ] Running all SQL scripts against data/financial_health.db succeeds without errors
</verification>

<success_criteria>
- Standalone analytical SQL scripts are complete and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-sql-analytics/04-01-SUMMARY.md`
</output>
