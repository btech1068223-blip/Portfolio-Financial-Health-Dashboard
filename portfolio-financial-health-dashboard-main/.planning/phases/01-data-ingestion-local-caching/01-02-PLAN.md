---
phase: 01-data-ingestion-local-caching
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/extract.py]
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03]
must_haves:
  truths:
    - "extract.py successfully pulls quarterly/annual statement JSONs and writes to data/raw/"
  artifacts:
    - path: "src/extract.py"
      provides: "Python extraction script using yfinance with sleep and cache options"
      contains: "yfinance"
  key_links: []
---

<objective>
Write extract.py script to fetch and cache raw yfinance statements.

Purpose: Pull and serialize quarterly/annual financials for SaaS companies to local JSON cache files.
Output: working src/extract.py script.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-ingestion-local-caching/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Create src/extract.py</name>
  <files>src/extract.py</files>
  <read_first>requirements.txt</read_first>
  <action>Write src/extract.py to loop through MSFT, CRM, ADBE, NOW, WDAY, DDOG, NET, HUBS, TEAM, OKTA. Fetch quarterly/annual income statement, balance sheet, and cash flow data using yfinance. Write to data/raw/{ticker}.json. Implement 1.5s sleep, try/except catching, and a cache check (unless --force flag is active).</action>
  <verify>python src/extract.py</verify>
  <done>src/extract.py runs successfully and saves JSON files to data/raw/</done>
</task>
</tasks>

<verification>
- [ ] python src/extract.py executes successfully without syntax errors
- [ ] data/raw/ contains JSON files for all 10 tickers
</verification>

<success_criteria>
- Raw JSON statements are cached locally for all companies.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion-local-caching/01-02-SUMMARY.md`
</output>
