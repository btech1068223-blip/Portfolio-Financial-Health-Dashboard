---
phase: 03-sqlite-storage-schema-setup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [sql/schema.sql, src/load.py]
autonomous: true
requirements: [DB-01, DB-02, DB-03, DB-04, DB-05]
must_haves:
  truths:
    - "load.py executes successfully and populates data/financial_health.db"
    - "SQLite database contains quarterly_summary and annual_summary views"
    - "TRIGGERS correctly update last_refreshed timestamp on updates/inserts"
  artifacts:
    - path: "sql/schema.sql"
      provides: "DDL schema definitions for tables, views, and triggers"
      contains: "CREATE TABLE"
    - path: "src/load.py"
      provides: "Database loading and population python script"
      contains: "sqlite3"
  key_links:
    - from: "src/load.py"
      to: "sql/schema.sql"
      via: "reads schema DDL to initialize database"
      pattern: "schema\\.sql"
    - from: "src/load.py"
      to: "data/processed/clean_financials.csv"
      via: "reads cleaned financials CSV"
      pattern: "clean_financials\\.csv"
---

<objective>
Design SQLite schema, create tables/views/triggers, and load clean data.

Purpose: Set up normalized SQLite tables and load clean tidy financials into them with views for dashboard consumption.
Output: sql/schema.sql, src/load.py, and data/financial_health.db.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-transformation-standardization/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Create sql/schema.sql</name>
  <files>sql/schema.sql</files>
  <action>Write database schema creation script sql/schema.sql containing:
1. companies table
2. financials table
3. quarterly_summary and annual_summary pivoted views
4. triggers to update last_refreshed</action>
  <verify>test -f sql/schema.sql</verify>
  <done>sql/schema.sql exists and contains SQL definitions</done>
</task>

<task type="auto">
  <name>Create src/load.py</name>
  <files>src/load.py</files>
  <read_first>sql/schema.sql, data/processed/clean_financials.csv</read_first>
  <action>Write src/load.py script to:
1. Connect/create data/financial_health.db.
2. Read sql/schema.sql and execute script to create schema.
3. Read data/processed/clean_financials.csv.
4. Extract unique tickers and populate companies table (mapping tickers to names: MSFT -> Microsoft, CRM -> Salesforce, ADBE -> Adobe, NOW -> ServiceNow, WDAY -> Workday, DDOG -> Datadog, NET -> Cloudflare, HUBS -> HubSpot, TEAM -> Atlassian, OKTA -> Okta).
5. Load financial rows into financials table. Use INSERT OR REPLACE for idempotence.</action>
  <verify>python src/load.py</verify>
  <done>src/load.py runs successfully and populates database</done>
</task>
</tasks>

<verification>
- [ ] python src/load.py executes without errors
- [ ] sqlite3 query confirms tables and views are populated
</verification>

<success_criteria>
- SQLite database is created, schema loaded, and data is ready for querying.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sqlite-storage-schema-setup/03-01-SUMMARY.md`
</output>
